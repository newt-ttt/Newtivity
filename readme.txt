REQUIREMENTS:
`node.js
 vite
 python (not yet mandatory)
 Django (not yet mandatory)`


To get everything up and running:
cd /client:
    run the following CMD commands:
        `npm install`
make a shortcut to start_activity.bat on desktop